name: Docker

on:
  push:
    branches:
      - dev

#  # Run tests for any PRs.
  pull_request:

env:
  # TODO: Change variable to your image's name.
  IMAGE_NAME: api
  DEV_IMAGE_NAME: api-dev
  IMAGE_OWNER: now-u

jobs:
  # Run tests.
  # See also https://docs.docker.com/docker-hub/builds/automated-testing/
  # test:
  #   runs-on: ubuntu-latest

  #   steps:
  #     - uses: actions/checkout@v2

  #     - name: Run tests
  #       run: |
  #         if [ -f docker-compose.test.yml ]; then
  #           docker-compose --file docker-compose.test.yml build
  #           docker-compose --file docker-compose.test.yml run sut
  #         else
  #           docker build . --file Dockerfile
  #         fi

  # Push image to GitHub Packages.
  # See also https://docs.docker.com/docker-hub/builds/
  build:
    # Ensure test job passes before pushing image.
    # needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Build image
        run: docker build . --file Dockerfile.deploy --tag ghcr.io/$IMAGE_OWNER/$IMAGE_NAME --label "runnumber=${GITHUB_RUN_ID}"

      - name: Log into registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Push image
        run: docker push ghcr.io/$IMAGE_OWNER/$DEV_IMAGE_NAME:latest
      
        # - name: deploy
        #   uses: appleboy/ssh-action@master
        #   with:
        #     host: ${{ secrets.HOST }}
        #     username: ${{ secrets.USERNAME }}
        #     password: ${{ secrets.PASSWORD }}
        #     port: ${{ secrets.PORT }}
        #     script: |
        #       cd ./traefik
        #       docker pull docker.pkg.github.com/now-u/now-u-api/api:${{steps.vars.outputs.tag}}
        #       docker service update --image docker.pkg.github.com/now-u/now-u-api/api:${{steps.vars.outputs.tag}} api_staging_api
